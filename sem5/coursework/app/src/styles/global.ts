import { StyleSheet } from 'react-native'

export const globalStyles = StyleSheet.create({
  input: {
    height: 40,
    width: '100%',
    borderWidth: 1,
    padding: 10,
    borderRadius: 8
  }
})
